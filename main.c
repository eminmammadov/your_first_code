// #include <stdio.h>

int main()
{
  // int i;
  // I have decided to use the loop variable "int i" only inside the loop. 
  // This approach makes the code cleaner and more compact. 
  // The shorter and more efficient the code, 
  // the more efficient it is in terms of RAM and CPU resources.
  for (int i = 0; i < 32; i++)
  {
    printf("Hello School\n");
  }
  return 0;
}
